

const Product = (props) => {

  console.log(props.product)
  // console.log(props.detectSale())

  // {props.productList.map((product, idx) => (
  //   <div key={idx}>
  //     <p>{props.detectSale() === product.category ? 'SALE' : ''}</p>
  //     <p>{product.name}</p>
  //     <p>{product.price}</p>
  //   </div>
  // ))}


  return (
    <div>

    </div>
  )
}

export default Product
